<!--
function aoihandler() {
	var tiles = new Array();
	this.d;
	var tile = {
		//tileId: '',
		wsIndex: 0, // websocket index that holds this sub-tile
		host: '', // host address
		h: 0, // height of the sub-tile
		w: 0, // width of the sub-tiles
		x: 0, // origin x coordination
		y: 0 // origin y coordination
	};
	this.init = function(){

	}
	// Add set of new tiles
	function addTiles(ts, wsIndex, host){
		this.d = ts;
	}
	// Get tile details
	function getTiles(){
		return ts;
	}
	// Remove set of tiles
	function removeTiles(tiles){
		
	}
};
// -->